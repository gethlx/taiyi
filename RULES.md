# 太乙枢机项目治理规则

- 状态：2026-09-03 个人病历与按职责父轮快照口径已确认
- 产品形态：供专业或进阶使用者个人使用的 Agent Skill

本文件只治理知识、目录、变更和实施边界，不重新定义理论或产品语义。

## 1. 权威顺序

发生冲突时按以下顺序处理：

1. `THEORY_CORE.md`：理论与推理规则原文；
2. `spec/THEORY_PROMPTS.md`：经人工确认的运行角色指令；
3. `PRODUCT.md`：用户任务、能力、结果和验收；
4. `ARCHITECTURE.md`：最小实施映射；
5. 本文件：文件与变更治理；
6. `spec/DEV.md`：当前活动切片；
7. 当前切片直接涉及的机器契约、代码和测试。

导航、旧计划、旧验收账本、代理意见和历史运行结果不能覆盖以上权威。
宿主记忆、历史运行目录、旧角色正文、旧 packet 和旧验收样例也不能建立本轮
任务身份、核对本轮医学答案或充当本轮职责结果；新任务只以当前对话、唯一匹配
的任务参考和新建权威快照为准。只有当前对话明确继续同一方剂或激活同一病例时，
才读取一份明确指定的上一正式记录维护身份和状态。

## 2. 目录职责

| 路径 | 职责 | 可否进入典籍检索 |
|---|---|---|
| `kb/texts/` | 每部典籍唯一有效的完整 Markdown | 可以 |
| `kb/assets/` | 正文配套图、说明和结构化数据 | 仅随所属正文 |
| `kb/manifest.json` | 作品、来源和 SHA 账本 | 不作为正文 |
| `refs/` | 原件、用户来稿和固定来源快照 | 不可以 |
| `spec/` | 当前提示词、机器契约、测试数据和开发交接 | 不可以 |
| `skill/` | 当前可交付的标准 Agent Skill 包 | 不可以 |
| `tools/` | 当前有效的确定性工具 | 不可以 |
| `legacy/` | 退出活动基线的旧代码、契约和过程资料 | 不可以 |
| `.workbuddy/` | 文档评审私有记忆 | 不可以 |

根目录只保留项目级权威文档、依赖声明和上述目录。活动代码不得导入、执行或失败回退到 `legacy/`。

## 3. 知识与来源

1. `kb/texts/` 中一个 `work_id` 只能对应一份当前完整 Markdown。
2. 原件、异本和来源快照留在 `refs/`，不得进入召回索引。
3. 调用卡和派生索引只负责路由与定位，不进入 `kb/manifest.json`，也不替代原文。
4. 返回直接证据前必须重新读取 `kb/texts/`。证据服务保留完整来源身份；医学
   角色只接收短摘录、典籍名称、文档路径和行号。
5. 完整原文、残文、证据冲突、未取回原文和模型泛化必须区分。
6. 缺字、异文和缺失图像不得无依据补写。
7. `01`—`09` 只是语料来源组，不是九层推理链或固定调用流程。
8. 每个职责只装入当前真正相关的短原文，默认不继承上一职责的来源集；正式
   结果再保存各职责实际采用来源的去重并集。不得建立累计来源包后无差别分发。

## 4. 模型与确定性工具

模型的预训练知识、知识涌现和泛化推理是主体。典籍只提供少量可信上下文、直接引文和核对边界。

确定性工具可以：

- 选择和装载已确认的角色指令；
- 在 R0 前冻结当前权威输入与明确父记录，并在制包和提交时核对同一哈希锁；
- 传递原话、身份、事实、关键短原文和完整上游职责结果；
- 调用宿主模型或子代理；
- 保存完整结果、来源身份和必要状态；
- 渲染同一结果的只读 H5。

权威输入中的 `scope` 继续用于提交守恒，但角色 packet 不把模板化 `scope` 文本
当成医学事实。病例完整原话与用户主动提供的体系外信息留在事实记录和正式记录，
不自动进入中医角色 packet。复察冻结一份有限 `case_snapshot`，再按职责删除
不适用历史内容；不调用模型另写摘要，不建立逐角色历史文件或证据图。

确定性工具不得用正则、词表、药性表、处方查询、固定评分或数据排列组合独立产生中医开放语义，也不得在模型失败时机械补写结论。

R0—R2 是因果职责，不是三个固定调用。当前方剂、病例和复察由宿主在同一
上下文中依次完成 R0、R1、R2；R1 获得完整 R0，R2 获得完整 R0、R1，随后必经
隔离红方；只有红方发现实质冲突时，宿主执行一次 Stage C。不保存 handoff，
不以另一种摘要替代完整上游结果。证据工具和保存操作不算推理角色。不建设
投票链、循环返工、模型网关、常驻代理系统、数据库、队列或多用户平台。

Stage 0 只表示宿主已经理解当前任务，不建立独立提示、packet、模型调用或公开
结果。已有正式结果后的普通解释不制造新 run；假设改方使用现有单沙盘；只有
新增病例事实、实际处方、治疗反应或明确正式替换才进入正式主链。上述判断由
宿主按语义完成，不建设关键词路由器。

纯方剂任务从显式组成完成方剂结构和条件性 R2，不以现实服用许可作为提交目标。
缺少只影响实际煎服的参数不能由编排层自动升级为实质冲突；同样，程序也不能
把未知操作补写成可执行建议。

## 5. Skill 实施尺度

1. 只实现直接服务个人 Agent Skill 旅程的能力。
2. 一个切片只增加一个可见能力；不为未来可能需求预建平台设施。
3. 宿主对话是输入、补充、纠正、病例激活和复察入口；不复制独立 Web 工作台。
4. 默认先交付完整文字结果；H5 和图像只在用户明确要求后扩展同一结果，不调用
   模型改变医学结论、不维护第二套医学状态。
5. 状态只登记明确病例文件、活动病例和一个沙盘；个人病历保存事实、轮次、实际
   方、治疗反应及正式结果链接，正式分析保存完整角色正文。普通文件与原子提交足够。
6. 新增角色、调用、状态或工具前，必须能指出它解决的真实个人使用问题；否则不增加。
7. 同一方剂只补充炮制、剂型或煎服时沿用 `formula_id`，只新建运行与结果身份；
   增减、替换或改量时才新建方剂身份，不建立版本图或谱系系统。
8. 新任务重新形成当前职责结果和来源选择；不得搜索宿主记忆、旧运行、旧验收
   样例中的医学内容来形成或核对本轮答案，也不得复用其中的角色正文、packet、
   来源集或候选结果。
9. R0 开始前冻结本轮权威输入及明确父记录；锁定后事实或方剂改变就废弃本次尝试，
   不在已完成角色结果下补改输入。
10. 单沙盘不改变病例或正式结果，也不能成为正式父轮；形成实质新方药候选时
    使用一个隔离红方，转成实际处方或正式结论时另起正式主链。

## 6. 测试与验收

机器测试只验证确定性边界：原话与主对象守恒、显式组成、同方补充信息时的
方剂身份连续性、冻结输入未变、复察父记录身份、来源短原文回读、病例隔离、
结果身份、正式／候选／沙盘身份、父上下文不携带候选与过程、未解冲突阻断和
失败不提交。H5 或图像实际生成
时另查同一 run/result 绑定，不阻塞默认文字提交。

开放语义按 `PRODUCT.md` 的小样本区间评审，不要求唯一答案或 100% 精确。Badcase 先查提示职责、上下文、证据边界或执行一致性的共同根因；不得增加样本专用关键词、方名必填、味数分支或其他词面补丁。

当前整改机器验收直接在 Codex 中运行，不使用 Computer Use 或外部代理。开放
语义由后续用户直接人工验收；H5 与图像待文字主链通过后另行设计和验收。

## 7. 变更与回滚

1. 修改前在项目外建立带时间戳的恢复点。
2. 只修改当前切片直接涉及的文件；同步更新 `README.md`、`spec/README.md` 和 `spec/DEV.md`。
3. 机器契约、代码与测试必须在已确认产品语义下建立，不能靠修改状态字段制造通过。
4. 知识正文或调用权如需变化，单独建立知识切片并请求人工确认。
5. 当前目录不是 Git 仓库；不在活动区复制“新版”“最终版”或第二套实现。
6. `legacy/` 只读保留。确需迁移时逐项复制到新实现并重新测试，不从旧入口直接调用。

## 8. 当前开发顺序

产品、理论核、角色提示词和实施架构已经人工确认。当前顺序固定为：

1. 隔离旧实现并验证保留的证据能力；
2. 从已确认文档重建紧凑机器契约；
3. 人工确认机器契约；
4. 创建最小 Skill 包；
5. 实现经典、方剂、病例、复察和只读 H5；
6. Codex 内确定性机器验收与用户直接文字验收；
7. 用户人工确认。

旧阶段计划、旧 Web 作品、旧压力规模和旧验收账本只能供追溯，不能授权或强迫当前实现。
