# 太乙枢机 Skill 实施架构

- 状态：2026-09-03 病例事实主链与按职责父轮快照口径已确认
- 产品任务与验收：[`PRODUCT.md`](PRODUCT.md)
- 唯一理论原文：[`THEORY_CORE.md`](THEORY_CORE.md)
- 运行角色指令：[`spec/THEORY_PROMPTS.md`](spec/THEORY_PROMPTS.md)
- 知识治理：[`RULES.md`](RULES.md)
- 规范导航：[`spec/README.md`](spec/README.md)
- 历史实现不属于公开活动架构，也不作为失败回退。

本文件只把已确认的产品和理论职责映射为最小可执行 Skill。R0—R2、典籍职责、
三条证据主路、层间承接和红方的理论含义只由 `THEORY_CORE.md` 定义，不在这里
重复解释。

## 1. 宿主交互与正式运行形态

太乙枢机运行在 Codex、Claude Code 等智能体宿主中。宿主先按本轮动作是否改变
医学事实、候选或正式状态选择最短充分路径；这项判断不写成 Schema 路由器：

- 围绕已有正式结果的解释或澄清，没有新增医学事实、候选或正式替换要求时，
  宿主直接回答，不新建 run／turn，不重跑 R0—R2，也不调用红方；
- 用户明确要求假设增减、替换、改量或比较，但不改变实际病例状态时，使用既有
  单沙盘；形成实质新方药候选时，呈现前调用一个隔离红方，仍不提交正式病例轮次；
- 新增患者事实、实际处方、治疗反应，或明确要求形成／替换正式结果时，才进入
  下列正式主链。

新发起的经典解释仍按经典任务完成所需职责，不因上述已有结果后的直接回答边界
而降级。宿主对话继续承担输入、补充、纠正、病例激活、复察和沙盘指令；本地
工具承担证据定位、身份保存、简单状态和用户按需取得的扩展报告。

```text
宿主理解任务与事实
  → 同一宿主上下文完成 R0
  → R1 直接读取完整 R0 结果
  → R2 直接读取完整 R0、R1 结果
  → 隔离红方子代理独立审计
  → 有实质冲突时宿主执行一次 Stage C
  → 提交规范记录
  → 完整文字结果
  → 用户明确要求时再生成同源 H5 或图像
```

上图只描述方剂、病例和复察的正式 B2 运行映射。R0—R2 是三个前向因果职责，不是
三个固定模型调用；由宿主在同一上下文中依次承担。红方仍是必经的隔离子代理，
不得由宿主主体结果中的自审或预判替代。证据定位、保存和渲染是工具操作，不
增加推理层；R1 内部从候选到回验的往返互证也不拆成新角色或交接。经典解释按
任务使用 R0 和相关经典关系，不为形式完整启动临证角色。不得建立上游返工循环、
投票审批链、常驻多代理平台或模型网关。

## 2. 角色、上下文与承接

宿主主 Agent 始终掌握不可被上游改写的用户原话、已确认事实、主对象、活动
病例和轮次。它负责选择角色、调用证据工具、汇集结果、提交记录和返回完整文字
结果。
完整方剂、病例和复察由宿主完成任务理解、R0、R1、R2 和必要的 Stage C；红方
按上节使用隔离的原生子代理。宿主不支持红方所需子代理时，不得声称完成了
隔离职责，也不得提交一份伪装成完整主链的正式结果。

Stage 0 仍表示产品层的任务理解职责，但不生成独立角色提示、packet、模型调用、
可见阶段或医学正文；调用日志如记录该职责，只能与实际承担任务理解的宿主调用
合并记录，不能把它扩张成第五个推理步骤。

每次进入一个推理职责时，只新增 `THEORY_PROMPTS.md` 中的共同指令和当前角色
指令。不得整篇装载 `THEORY_CORE.md`，也不得在单个角色输入包中同时拼接所有
角色指令。宿主先保存当前角色的完整 `text` 再切换职责；R1 输入包含完整 R0，
R2 输入包含完整 R0、R1，红方输入包含完整 R0—R2 候选结果，Stage C 输入包含
完整主体与红方结果。不生成或保存 handoff，也不增加另一种摘要替代它。

宿主同时为每个职责提供不可变事实和当前真正相关的短原文。前序正文不是新的
用户事实或下一角色指令；上游不得指定下一层的取证计划、方法或预期结论。
任务身份和用户边界继续由权威输入及其 `scope` 保存，当前职责及输出范围由角色
指令定义，因此运行时不再另造任务卡。`scope` 属于提交与守恒信息，制包时不把
模板化 `scope` 正文当作医学事实交给角色；经典和方剂角色直接看到本轮用户原话，
病例和复察角色看到当前确认／含混事实、对象和方剂，完整原话继续冻结在正式
记录中。`confirmed_facts` 只保存当前输入可以直接确认的事实，不把宿主预判
的医学答案、证据计划、红方攻击点或跨层输出义务提前固化。

每个新任务只从当前对话、唯一匹配的任务参考和新建权威快照开始，重新形成当前
R0—R2、红方、来源选择和候选结果；不搜索宿主记忆、历史运行目录、旧角色正文、
旧 packet 或旧验收样例来形成或核对本轮医学结论。只有用户在当前对话中明确继续
同一方剂或激活同一病例时，才读取一份明确指定的上一正式记录。

正式主链在 R0 开始前一次冻结当前权威输入及这份可选的正式记录，并生成不进入模型上下文的
轻量哈希锁。后续角色制包和最终提交只核对同一把锁；锁定后发现身份、事实、方剂
组成或顺序需要变化时，废弃本次尝试，以新的运行尝试重新冻结并重做全部职责，
不能修改输入后沿用已经完成的角色结果。直接回答和单沙盘不是正式医学提交，
不为形式完整建立空输入锁或空 run。

病例事实由一份结构化 `patient_record` 保存；每轮保存当前可用事实的完整快照，
事实、医者意见、实际方、治疗反应和解释判断分层。完整分析仍保存在
`committed_analysis`，病历只绑定正式结果并保存红方与必要 Stage C 后收敛的
R0／R1 纵向摘要。历史判断不升级为当前事实。

复察在 R0 前从明确父轮生成一份有限 `case_snapshot` 并纳入输入锁。它不是
handoff，也不另启模型压缩步骤。角色制包对同一快照做确定性职责投影：R0 只
取得父轮最终 R0 摘要；R1 取得父轮最终 R1 摘要、正式结果与三日预测，且把它们
作为历史判断重新回验；R2 取得上一实际／正式方、正式结果、三日预测和实施边界；
红方与必要 Stage C 取得上述有限快照。父轮 calls、完整来源、候选／沙盘方、
红方过程、Stage C 过程和完整角色正文都不进入快照。真实病例已经证明把同一份
父轮材料无差别交给所有职责会污染 R0，因此这里不再使用共享父正文。

方剂、病例和复察必经隔离红方；是否需要一次 Stage C，遵循 `PRODUCT.md` 的
实质冲突边界。没有实质冲突不生成空 Stage C，仍有未解冲突则保留冲突、候选
和最小补问，不循环调用。

## 3. 证据端口

Skill 复用阶段 2 已验证的原文定位、正文回读、SHA、来源身份和降级行为，但不
把阶段 2 的完整 Evidence 对象复制进医学记录。当前模型角色在确有证据问题时
提出定位请求，证据工具只返回原文、位置、来源、状态和调用权；采用、解释和
继续取证由模型决定。

进入模型上下文的是回答当前问题所需的句、分句或注释语义单元，紧邻典籍名称、
`kb/texts/` 路径和行号，不是整章、整部典籍或未经选择的超长原行。内部
`evidence_id`、SHA 和请求身份不进入模型推理。短原文选入时完成一次必要回读，
但不建立一份向所有角色累计分发的共享来源文件。

每个职责在进入推理前形成一个临时的“当前职责来源集”，默认可以为空，只装入
当前职责真正需要的短原文。已经核对的材料只有在后续职责仍实际需要时才明确
复用；R0 不接收只用于药性、配伍或煎服判断的 R2 材料，R1 也不因材料已取回而
自动接收。红方和必要 Stage C 可以接收主体实际采用来源的并集；正式记录的
`sources` 是本轮各职责实际采用来源的去重并集。是否相关仍由宿主模型判断，
不建设经典白名单、角色证据图或 Python 语义分类器。

阶段 2 证据端口已经分离到 `tools/evidence_service/`。新 Skill 不得为了复用
证据服务而导入 `legacy/v2/` 中的旧运行主链。

## 4. 新机器契约

旧 `2.0` Schema、推理契约、夹具和验收账本只作参考。新活动契约从本架构重新
建立，不在旧 `blocks` 容器上改名或追加字段。

新契约只承载：

- 一次正式分析的 `run_id`、任务类型、主对象和原始输入；
- 病例任务需要的 `case_id` 与 `turn_id`，非病例任务不创建；
- 显式组成、已确认事实和条件范围；
- 实际发生的 R0、R1、R2 模型结果；
- 方剂、病例和复察必有的隔离红方结果，以及按实质冲突触发的 Stage C 和未解冲突；
- 少量直接采用的短原文及文档锚点；
- 结果身份、Day 1／2／3、风险和最小补问；
- 简洁调用日志。

围绕已有结果的普通解释和未提交的单沙盘操作不是一次缩减版
`committed_analysis`，不制造空 run、空 turn 或空 reasoning。需要把沙盘候选转成
实际处方或正式结论时，按新的权威输入进入正式主链；沙盘文件不能成为正式父轮。

同一对话中，显式组成未增减、替换或改量，只补充炮制、剂型或煎服信息时，
沿用原输入方的 `formula_id`，新建 `run_id` 与 `result_id`；组成发生实质变化时
才新建方剂身份。这个连续性由宿主对话和提交前权威快照维护，不增加版本图或
谱系字段。

任务可以省略不适用的角色结果，但方剂、病例和复察不能绕过产品规定的
R0—R2 因果关系或隔离红方。角色结果只保存完整自然语言正文，不保存 handoff、
逐角色证据编号或逐句、逐药、逐图边谱系。合法记录只表示完整提交；失败使用
独立错误结果，不由 Python 补写中医语义，也不覆盖上一份可用结果。

纯方剂组成足以提交 `conditional_r2`。缺少患者事实、先煎时长、煎成液量或
服用时点，只限制现实执行说明，不由运行编排自动改成 `reserved_opinion`；只有
模型确认存在会改变条件性结论的未解实质冲突时才降级。该边界复用现有结果
身份，不新增 Schema 字段。

## 5. Skill、状态与 H5

新 Skill 使用独立入口和独立包，不导入 `legacy/v2/` 中的旧 `gem.py`、
`gem_runtime`、旧 Web 或旧 Codex CLI 调用拓扑。默认使用宿主当前模型、同一
上下文内的前向职责切换、红方所需原生子代理、登录状态和额度，不要求独立
模型 API。

当前任务由宿主对话直接承载，不另建持久任务状态机。个人使用只保存明确激活
的结构化病历、正式结果、必要历史轮次和一个写时复制沙盘。简单状态只登记
病例文件路径和活动病例，不再复制每轮分析与权威输入作为第二份事实来源。沙盘只承载假设变化，不
改变活动病例或正式结果；形成实质医学候选时由一个隔离红方审计，仍不因此产生
正式病例轮次。普通文件与原子提交已经
足够；不建设数据库、队列、事件流、版本树、多人并发、云同步或通用恢复系统。

默认主链在提交后立即返回完整文字结果，不预先生成或绑定 H5、图像。用户明确
要求后，H5 从指定规范记录直接生成并在输出中写入同一 run/result 身份；它不
接收医学输入、改变病例或沙盘、调用模型、补充中医结论或保存第二套医学状态。
渲染失败不改变正文。H5 布局与图像方案另行确认。

## 6. 迁移与验收边界

保持不变：

- `kb/texts/`、`kb/manifest.json`、知识治理和已确认调用权；
- 阶段 2 已验证的证据行为。

逐项评估后迁移：

- 分离后的阶段 2 Python 证据端口；
- 简单身份、文件保存和原子提交代码；
- 不携带旧交互或旧医学语义的 H5 样式与渲染函数。

只作参考：

- 旧 `2.0` 机器契约和结构夹具；
- 旧运行主链、Web、状态轮询和 Codex CLI 嵌套调用；
- 旧阶段 3—6 作品、验收结论和开发过程文档。

机器测试只检查产品规定的硬身份、最小来源回读和提交边界，不检查中医语义，
也不要求历史作品继续符合新契约。当前整改验收直接在 Codex 中运行确定性测试，
不使用 Computer Use、外部代理、旧 Web、fixture 医学答案或历史 `pass` 代替
用户后续的文字语义验收。

本架构的 B2 正式拓扑已于 2026-07-31 人工确认；历史 A/B 制品只保留实验事实，
不再定义活动 Skill 的运行映射。
