# 太乙枢机产品场景与能力规范

- 状态：2026-09-03 个人病历事实层与复察解释层口径已确认
- 已纳入的人工决定：D1—D9、R0—R2、动态红蓝审计、宿主智能体模型／额度边界、R2 医师复核口径、六维可演化与开放图形解释、H5／图像对外展示纯净版
- 理论核：[`THEORY_CORE.md`](THEORY_CORE.md)
- 分角色提示词：[`spec/THEORY_PROMPTS.md`](spec/THEORY_PROMPTS.md)
- Skill 实施架构：[`ARCHITECTURE.md`](ARCHITECTURE.md)
- 规范导航：[`spec/README.md`](spec/README.md)
- 当前机器契约状态：旧 `2.0` 记录、推理契约、夹具和验收账本只作参考；活动机器契约须依据本文件、人工确认的理论核和分角色提示词重新建立，不能沿用旧结构后改名或打补丁

本文件规定产品目的、用户任务、语义职责、结果边界和验收方式。它不规定固定模型调用数、代理编排、页面组件或通用平台能力。

## 1. 产品本质

太乙枢机是供具有中医基础、能够自行复核结论的专业或进阶使用者个人使用的辅助分析工具。最终交付形态是 Codex、Claude Code 等智能体工具中的 Skill，而不是要求用户另行部署模型服务的独立平台。它以宿主智能体的预训练知识、知识涌现和关系泛化为主要推理能力，以本轮用户事实和少量相关典籍原文作为可信上下文，完成经典解释、方剂分析、病例辨证和同案复察。

默认使用宿主智能体当前可用的模型、子代理能力、登录状态和额度。产品不得把独立模型 API、额外 API Key、第三方模型账户或额外计费配置设为安装、运行或验收前提；用户主动选择的外部模型接口只能作为可选扩展。

Codex、Claude Code 等宿主 Agent 对话是正式交互入口，承担自然输入、补充、纠正、病例激活、同案复察和沙盘指令。Skill 读取本轮对话与必要病例状态，调用本地证据、身份、保存和渲染工具；不再复制一套 Web 输入、病例操作或任务轮询界面。

默认先在宿主对话中交付完整文字结果。文字完成后可以告知使用者：如有需要，
可继续生成同源移动端 H5 或图像解释；只有用户明确要求后才生成，不能让它们
延长默认文字等待。H5 只读表达已经提交的同一结果，不接收新的医学输入、确认
或改写事实、切换活动病例、修改沙盘、启动模型调用或产生新的中医语义。后续
追问和纠正继续在宿主 Agent 对话中完成。

典籍知识库不替代模型的预训练知识，也不负责机械拼装答案。它只承担：

- 校准经典调用权；
- 提供少量相关原文和反面证据；
- 标明作品、位置、来源与证据边界；
- 在模型推理后支持使用者核对。

`kb/texts/` 是唯一典籍正文入口；调用卡、派生索引、摘要和模型泛化都不能替代原文。

模型可以基于本轮原文锚点和自身预训练知识完成跨段、跨书和新输入上的涌现与泛化，但必须区分经典原文、跨书关系和解释结论。直接引文用短原文和文档锚点证明；解释结论自然表达，不要求逐段重复模型来源免责声明。检索数量、逐句覆盖率、字段数量和 ID 数量都不是作品质量目标。

这里按**语义来源**而不是按“程序／非程序”划界。宿主主代理、隔离子代理以及由 Skill 调用的模型都属于模型推理过程；Python、正则、检索器、Schema 和渲染器等确定性工具可以构造模型上下文、发起模型调用、提出非权威候选、定位原文、校验身份、保存和呈现模型结果，并执行用户或模型已经明确给出的结构化操作。涉及任务对象、方义、病机、治法、药物职责、审计结论、Stage C、方药建议和三日演化时，它们不得仅凭词面命中、查表、相似度、固定阈值或数据重组自行得出结论；新增语义和冲突裁决必须由模型完成。程序可以生成最终文件、页面和无语义汇总，但不能在模型失败时补造中医答案，也不能按多数票、固定权重或最高分替模型裁决。

## 2. 模型推理主权与核心原则

1. **事实主权**：保存本轮原话和显式组成；方名、历史相似结果和经典模板不能覆盖本轮输入。
2. **模型语义主权**：任务、对象、病机、治法、方义、药物职责、审计和综合结论由模型形成。
3. **原文锚定与模型泛化并存**：核心经典关系回到真实原文；外部知识库不承担逐项覆盖全部开放语义的责任。
4. **Stage 0/A/B/C 能力完整、按任务适用**：方剂分析、实际病例和复察覆盖完整红蓝综合职责；经典研读按问题使用理解、解释与综合，不为形式完整强制启动临证 Stage B/C。Stage 名称不是固定调用边界、固定代理数或固定 JSON 片段数。
5. **适应性红军审计**：原 GEM 的属性、结构、方向、时位、承载、元典与经典关系通常合称“六个维度”，是当前默认审计底座和历史基线，不是不可修改的机器本体。模型可以按任务把不适用项条件化，合并相近视角、增加任务特有视角，或采用更能暴露冲突的组织方式；验收看是否真正攻击蓝军并覆盖当前关键风险，不看是否恰好输出六个固定字段。
6. **Stage C 综合**：任务已经启动红军审计时，Stage C 吸收攻击并形成综合；红军发现实质问题时完成重校、候选调整或保留冲突，没有实质冲突时不建立空的复审谱系。未启动红军的经典研读由主代理直接形成可见综合，不为名称完整制造 Stage C 记录。
7. **三日演化**：方剂、病例和复察均说明服后第一、第二、第三日（Day 1、Day 2、Day 3）；纯方剂结果是带适用前提的条件性预测，不冒充已经发生的病例事实。
8. **中医体系内运行**：用户提供的体系外名称和指标只保留在隔离原话中；主体模型、红方和 Stage C 也不得主动补入用户未提供的现代诊断、指标、筛查框架、风险分类或裁决原则。这些内容不进入中医事实、R0／R1／R2、审计、图形或结论。若以后确需独立的非中医提示，必须先确认其独立身份和显示边界，不能静默混入中医分析链。
9. **方向未定事实继续运行**：方向、程度、时点和相对状态不清时保留原话、合理范围和条件分支，降低可信程度但不自动阻塞。
10. **使用者负责最终决策**：系统给出明确、详细且可核对的辅助建议；审计通过不等于机器批准处方。
11. **分析深度服从任务**：纯方剂任务形成条件性方义与演化，不把现实煎服许可
    设为隐含终点；实际病例、复察或用户明确要求的具体服法才进入相应执行层。

### 2.1 R0—R2 产品义务

R0、R1、R2 的理论含义、因果关系、典籍职责和取证尺度只由
[`THEORY_CORE.md`](THEORY_CORE.md) 定义，本文件不建立第二份理论解释。

产品必须使需要主体中医分析的任务实际体现 R0 本层关系与范围、R1 对完整 R0
的独立承接，以及 R1 经治法进入 R2 的因果关系。R0 不替 R1 指定辨证方法、
取证计划或预期结论。经典研读按问题决定是否进入 R1、R2；方剂、病例和复察按
各自事实范围使用完整因果关系。纯方剂可以形成条件性的 R2 治法、方义、药物
职责、与方剂结构有关的炮制／剂型影响和修订候选，但不能冒充现实病例的
`R2 详细建议（待医师复核）`。这些关系由真实作品验收，不用关键词、固定字段、
固定调用数或唯一答案判定。

## 3. Stage 与动态红蓝代理

### 3.1 语义职责

以下是系统具备的职责地图，不要求四类任务机械实例化全部 Stage：

- **Stage 0**：理解本轮任务、主对象、参考／比较／否定关系、事实范围和必要的证据定位问题。
- **Stage A（蓝军）**：在本轮事实、关键原文和模型知识上完成任务所需的 R0、R1、R2 或经典解释。
- **Stage B（红军）**：隔离地攻击 Stage A，以原六维为默认起点，并按当前任务补充或重组审计视角，寻找冲突、反例、遗漏和条件边界。
- **Stage C**：吸收红军结果，完成综合、必要的重校、新候选、保留意见和三日演化。

一个宿主上下文可以承担相邻 Stage。方剂、病例和复察的 R0—R2 由宿主同一
上下文完成，红方使用一个隔离子代理，必要 Stage C 回到宿主综合。产品验收
检查职责是否真实完成，不以模型种类或固定调用数判断质量。

### 3.2 审计适用边界

经典研读在原文相冲、调用权不清或用户明确要求时增加隔离审计。方剂分析、
实际病例和同案复察必须有一次独立红方审计；只有红方发现实质冲突时才由宿主
执行一次 Stage C，不增加第二轮红方、投票或循环重校。

实质冲突只指会改变核心关系、方剂主轴、药物身份、来源身份、结果身份，或使
当前条件性结论本身不能成立的问题。纯方剂分析中只影响现实操作的煎煮参数、
服用时点等未知项属于执行边界，不自动触发 Stage C 或结果降级。红方仍须阻止
模型把未知服法写成现实可执行指令，但不能把方剂分析改造成“服用批准”审查。

产品要求红蓝上下文隔离、默认继承宿主智能体的模型与额度，也允许宿主在已有
能力内选择其他模型；但不以模型种类、代理数量或调用次数判断通过。具体由哪个
角色承担 R0、R1、R2、红方和 Stage C，属于
[`ARCHITECTURE.md`](ARCHITECTURE.md) 的运行映射，不能反向改变本节的语义职责。

## 4. 四类用户任务

### 4.1 经典解释 `classic_interpretation`

- 输入：原文片段、概念、理论问题或跨书关系。
- 过程：定位相关原文，按经典层级与调用权解释，区分原文、注解、跨书关系和模型综合。
- 输出：直接结论、关键原文、解释链、适用边界；可以按当前解释价值或用户要求生成开放图形解释。
- 边界：主体解释以 R0 为元典根基；没有治法方药问题时不为形式完整启动 R2。

### 4.2 方剂分析 `formula_analysis`

- 输入：一味或多味组成即可；方名不是必填项。剂量、炮制、煎服、目标和参考方按用户实际提供程度保存。
- 过程：以显式组成为主对象完成适配的 Stage 0/A/B/C；R0 形成一气、阴阳、
  《周易》可能对应关系及其范围，R1 反推条件性核心证机与方证，R2 先完成治法、
  方义和方内职责，再解释确会改变药物身份、方内权重或结构的剂量、炮制与剂型
  影响。现实煎服只在用户明确要求时进入相应深度。
- 输出：主对象、条件性方义与方证、药物职责、红蓝审计、必要 Stage C、差异和
  候选、风险、关键原文及 Day 1/2/3 条件预测。
- 边界：没有人体事实时不虚构现实患者病机、承载或疗效，也不产生服用批准。
  煎服信息缺失可以限制现实执行说明，但不阻断方剂结构、条件性 R2 和三日预测；
  输入方和候选方不能升级为现实病例 R2 详细建议。

### 4.3 实际病例 `case_reasoning`

- 输入：自然病例叙述、症候、舌脉、病程、既往处置、治疗反应、时位及身体条件。
- 过程：先把本轮原话、已确认／含混事实、实际方和治疗反应写入个人病历事实层；
  用户主动提供的体系外信息单独保存，不自动进入中医角色上下文。模型按 R0 元典
  关系与范围→R1 辨证求机→R2 治法方药完成主体分析，再由红方审计并在必要时
  进入 Stage C；提交后才把收敛后的 R0／R1 纵向摘要和正式结果链接写回本轮。
- 输出：本轮事实、成立范围、病机、治法、详细方药建议、审计、风险、三日演化、关键原文和复察条件。
- 边界：事实不足时继续给当前可成立的分析和最小补问；未解实质冲突时不产生 R2 详细建议。

### 4.4 同案复察 `followup`

- 输入：明确激活的病例以及本轮新增事实和治疗反应。
- 过程：本轮病历保存一份当前可用事实的完整快照，并明确连接同案上一轮；方向
  未定项不覆盖既有事实。正式运行只冻结一份有限病例快照：R0 读取父轮最终 R0
  纵向摘要，R1 把父轮最终 R1 纵向摘要和预测视为历史判断而重新辨证，R2 读取
  上一实际／正式方与必要治疗历史。任何父轮判断都不是本轮事实或预定结论。
- 输出：预测—实况对照、变化分类、守方／调整／转方／重辨判断、方药差异和新的三日演化。
- 边界：新病例从空白开始；不按症状或方名相似度猜测病例身份。

同一输入可以包含多个意图，但一次只激活一个用户任务。当前任务内部实际启用的检索、蓝军、红军和 Stage C 属于一条语义链，不作为多个排队任务；未适用的职责不生成空任务或空记录。

## 5. 对象、证据和结果身份

### 5.1 输入与方剂身份

必须区分：

- 用户输入方；
- 参考经典方；
- 比较或仅提及／被否定的方；
- Stage A 或 Stage C 候选方；
- R2 详细建议（待医师复核）；
- 写时复制沙盘方。

显式组成、顺序、剂量、单位、炮制和煎服不得因方名、括号、引述、否定或标点而丢失。对象存在会改变主任务的实质歧义且模型也不能可靠判断时，才请求一次可见确认。

同一对话中，显式组成没有增减、替换或改量，只补充原方的炮制、剂型或煎服
信息时，保留原 `formula_id` 和中性的主对象标签，只产生新的 `run_id` 与
`result_id`。新增、删除、替换药味或改变剂量时才建立新的方剂身份。这里不建立
方剂版本图、谱系字段或数据库。

新任务只从当前对话、唯一匹配的任务参考和新建权威快照开始，不为形成或核对
本轮医学结论搜索宿主记忆、历史运行、旧角色包或旧验收样例。只有用户在当前
对话中明确继续同一方剂或激活同一病例时，才读取一份明确指定的上一正式记录；
其中需要继承的身份、事实、方剂和结果边界必须在 R0 前进入本轮冻结输入或父轮
正式上下文，当前 R0—R2、红方、来源选择和候选结果仍重新形成。

### 5.2 证据身份

检索结果区分：

- 完整原文；
- 残文；
- 未取回原文；
- 证据相冲；
- 跨书关系候选；
- 模型泛化。

直接引用在正式医学记录中保存 `work_id`、标题、正文路径、行号和逐字短摘录；
证据服务在选入时另用清单 SHA 回读核对，SHA 与内部证据编号不进入模型推理或
角色结果。只要求与核心关系直接相关的小型原文子集，不要求每个句子、每味职责、
每个审计发现或每条图边建立独立证据对象。模型泛化不能伪装成原文。

### 5.3 R2 详细建议口径

R2 详细建议表示模型在当前事实、R1 辨证、审计和 Stage C 基础上形成的详细方药建议，不表示程序或模型已经批准其安全性和现实执行。

面向使用者统一显示为：

- `R2 详细建议（待医师复核）`；
- `条件性 R2`；
- `事实或证据不足`；
- `审计后仍有保留意见`。

纯方剂任务可以形成 `条件性 R2`，包含治法、方义、药物职责、与结构有关的
炮制／剂型影响和修订候选，但不产生现实病例的
`R2 详细建议（待医师复核）`。缺少患者事实或完整煎服参数本身不要求改为
`审计后仍有保留意见`；只有未解问题会改变方剂主轴、药物身份、来源真实性，
或使条件性结论本身无法成立时，才使用保留意见或事实／证据不足。只有实际病例
或同案复察在当前事实支持且没有未解实质冲突时，才可以产生详细建议。

## 6. 六项核心能力

- **C01 事实确认**：保存原话与中医事实候选；用户在宿主对话中纠正，Skill 基于新事实版本重新推演受影响结论。
- **C02 病例与时间轴**：病例激活、续案和切换由宿主对话承载；个人病历分开
  保存患者事实、医者意见、实际方、治疗反应和审计后 R0／R1 纵向摘要；病例、
  轮次和历史方案严格隔离，A→B→A 不串案。
- **C03 方剂差异与受控化裁**：在宿主对话中接收明确的保留、增减、替换、改量、炮制和煎服指令；一个沙盘不覆盖输入方、候选方或 R2 详细建议。
- **C04 分层结果与证据**：宿主对话在执行中给出真实阶段进度，完成后先显示
  唯一正式结论，再完整保留 R0／R1／R2、红方、必要 Stage C 和关键原文；H5
  只在用户明确要求时扩展同一结果。
- **C05 三日演化与复察**：方剂、病例和复察均输出 Day 1/2/3，并清楚区分条件预测、病例预测和已经发生的变化。
- **C06 图形解释**：图形是基于同一输入、证据和推理上下文的第二种解释作品。它不只是机械投影，也不是分析成立的固定门槛；模型可以对文字结论进行重新组织、关系展开和有价值的语义补充，不要求每个图节点或关系都已逐字出现在正文中。

### 6.1 图形选择建议

| 用户任务或解释目标 | 优先考虑的图形 |
|---|---|
| 经典研读 | 概念层级图、典籍关系／调用权图、气化过程图、原文—解释关系图 |
| 方剂分析 | 药味—职责—治法关系图、方内结构图、升降出入／开阖枢动态图、方剂差异图、三日演化图 |
| 实际病例 | 病机链、气化与阴阳消长图、脏腑—经络—病位关系图、事实—病机—治法—方药链、条件分支图 |
| 同案复察 | 前后变化图、预测—实况对照图、守方／调整／转方／重辨路径图、方药差异图、新三日演化图 |
| 跨任务核对 | 关键证据关系图、经审计收敛后的 R0／R1／R2 层次图，以及模型认为更适合当前问题的自由组合图 |

这些类型是推荐起点，不是封闭枚举。默认可以不生成图；只有用户明确要求，或一张图能明显降低文字理解成本时才生成。模型可以选择一种或多种，也可以提出新的图形类型或组合形式；是否生成、生成多少和采用何种形式由当前解释价值、用户要求和移动端可读性共同决定，不设固定张数。H5 报告优先采用可纵向阅读、标签清楚、可展开细节的移动端布局，但不把固定视口尺寸作为正式交互门。

红方攻击与 Stage C 修复过程不进入默认 H5 或图片。只有用户明确要求解释审计
过程时，才可以另行生成红蓝攻防或审计修正图，并继续区分审计前候选、已排除
内容、修正后结果和未解项；这类图不能替代下节规定的对外展示纯净版。

图形扩展出的中医关系必须来自模型推理，并与本轮输入、证据边界和主体结论保持可理解的联系；它可以超出文字正文的逐句内容。新增的重要解释应同时提供简短可读的图形说明，不能伪装成经典原文，也不能在图中静默覆盖主对象或用户事实。若图形推演使关键结论发生变化，应把变化显式带回 Stage C 或正文补充，而不是只藏在图里。固定人体底图、PNG 和 PDF 后置。

已经确认的通用视觉生成原则是：**太乙枢机项目母体提供稳定审美身份，最终 R0
提供每份报告独有的动态视觉母题，R1／R2 与最终结果决定信息图结构，H5 与图片
再按媒介特性适配**。R0 应先提炼为象、位、势、机、度等可核对视觉语义，而不是
直接按关键词配卦、配色或套图；具体卦象只有在最终 R0 已明确给出对应关系和边界
时才可使用。完整方案与验收要求见 `spec/H5_VISUAL_EXPERIENCE_ANALYSIS.md`。

## 7. H5 与图片的对外展示纯净版

### 7.1 产品决定与数据流

完整文字报告负责保存唯一正式结论、完整 R0／R1／R2、红方攻击、必要
Stage C、未解冲突、三日内容和短原文来源，供使用者完整阅读与复核。H5 与图片
不直接消费这份运行全过程；用户明确要求视觉扩展后，必须先从同一已提交结果
形成一份**对外展示纯净版报告**，内部称 `presentation_projection`：

```text
committed_analysis
  → 最终表达归一化
  → presentation_projection
  → H5 适配／图片生成
```

纯净版把“红方与必要 Stage C 如何处理主体候选”收敛为唯一最终表达。它是同一
正式结果的只读对外投影，不是第二份医学结论、第二个病例状态或第二张方剂。

### 7.2 身份与守恒

纯净版必须绑定原记录相同的：

- `run_id`、`result_id` 和 `task_type`；
- 主对象、病例与轮次身份；
- 用户原话、已确认与含混事实及成立范围；
- 方剂身份、组成、顺序、剂量、单位、炮制和煎服；
- 最终结果身份、三日身份、短原文和来源。

纯净版不得新建 `result_id`、`formula_id` 或医学结论身份，不得覆盖、删除或改写
原 `committed_analysis`。它只作为 H5 和图片的内容入口与同源约束。

### 7.3 保留、吸收与排除

纯净版保留：

- 最终主对象、事实范围和适用边界；
- 经红方与必要 Stage C 收敛后的 R0／R1／R2 或经典解释链；
- 最终方剂及其真实角色；
- 最终 outcome、风险、最小补问和未解实质冲突；
- 经修正后的 Day 1／2／3；
- 仍支持最终表达的短原文与来源；
- 一份简洁的独立红方检验回执。

纯净版不带入默认对外视觉叙事：

- 红方攻击全文；
- Stage C 原始修复过程；
- 已被 Stage C 修改、排除或降级的审计前候选；
- 已失效的方剂、结果身份、三日表达或来源解释；
- 内部调用、角色交接和模型调试信息；
- 只对过程复核有价值、对最终理解没有价值的重复内容。

完整报告继续原样保留这些审计材料。纯净版只是默认不向 H5 和图片传播失效或
已被替代的中间表达。

### 7.4 独立红方检验回执

纯净版不使用笼统的“检验通过”，也不把审计描述成安全批准、服用批准或医师
复核完成。按真实收敛状态只生成以下一种公开回执：

| 状态 | 对外回执 |
|---|---|
| 当前任务未启动红方 | 不显示红方标识 |
| 红方未发现实质冲突 | 已完成独立反向检验，主体结论保留 |
| 红方发现问题并由 Stage C 收敛 | 已完成独立反向检验与综合修正，以下为修正后结果 |
| Stage C 后仍有未解项 | 已完成独立反向检验，仍有未解项，本轮保留相应边界或意见 |

H5 可以显示这份回执及最终未解边界；普通可视化图片只保留必要的简洁状态标识。
用户明确要求审计解释时，才另行生成审计过程作品。

### 7.5 归一化职责与失败边界

最终表达归一化属于语义整理，不属于前端排版。无红方或红方无实质冲突时，主体
正式内容可以直接进入纯净版；存在 Stage C 时，纯净版必须吸收其保留、修改、
排除和未解决内容，并确保失效候选不再进入 H5 主叙事、图形节点、图片提示词或
摘要长图。

前端、Schema、正则、关键词、药性表、固定映射和最高分选择都不能自行裁决红方
与 Stage C 的语义。H5 渲染器只读取已经归一化并通过同源、守恒和结果身份校验
的纯净版，继续保持确定性、只读和不调用模型。若当前记录不能可靠形成唯一最终
表达，H5 与图片生成失败关闭，不由渲染器猜测或拼接。

### 7.6 当前实现状态

本节是已经人工确认的产品决定。当前 `spec/analysis.schema.json`、
`skill/taiyi-shuji/references/h5-report.md` 与 `render_h5.py` 尚未实现
`presentation_projection`；它们直接读取完整规范记录的行为不能作为本节已经
落地的证明。后续必须先建立纯净版机器契约、生成职责、守恒校验和失败边界，再
调整 Skill 与 H5／图片生成流程。产品决定确认不等于实现完成。

## 8. 最小运行记录

统一记录只保存用户和后续运行真正需要的内容：

- 原始输入及不可变显式组成；
- 当前任务、主对象、病例和轮次身份；
- 已确认事实、模糊事实及成立范围；
- 关键原文与证据状态；
- 蓝军主要结论；
- 红军适应性审计发现；
- Stage C 综合、候选和保留意见；
- R2 详细建议或条件性结果；
- Day 1/2/3；
- 用户明确要求后产生的 `presentation_projection`、图形与 H5 作为绑定同一
  run/result 的扩展作品，另行保存，不预建在默认医学记录中。

模型、实际职责、调用状态和错误可以保存在简洁运行日志中。输入／输出哈希、
Schema 哈希、沙箱方式和传输细节属于调试信息，不成为每个医学结论、药味职责、
角色结果或来源的必填业务字段。

合法规范记录只表示一次完整提交。模型失败、输出非法或未解决实质冲突使用明确错误／条件结果，不用 Python 补写一份中医答案，也不覆盖上一份可用结果。

### 8.1 个人病历

病例与复察另有一份简单个人病历，保存用户原话、当前事实快照、含混事实、用户
主动提供但与中医推理隔离的外部信息、医者意见、实际方、治疗反应、明确更正、
正式 run/result 链接，以及红方与必要 Stage C 后已经收敛的 R0／R1 纵向摘要。

完整角色正文仍只保存在正式分析记录；纵向摘要是可回看的历史判断，不是患者
事实。未来 R0 只取得父轮最终 R0 摘要，R1 只把父轮最终 R1 摘要作为历史假设，
R2 取得必要治疗历史；红方取得上述有限材料。事实写入可以先于分析，分析失败
不删除事实，也不产生医学结果绑定。

病历使用普通结构化文件和原子写入，不建设数据库、账户、自动病例匹配、完整
电子病历、事件图或第二套医学状态。人类可读视图如有需要由同一结构化病历
确定性生成，不与手工维护的目录索引形成双重权威。

## 9. 验收层级

### 9.1 可机器验证的不变量：必须 100%

- 原始输入、显式组成、剂量、炮制、煎服和来源跨度不被静默改写；
- 同一方剂只补充炮制、剂型或煎服时保留 `formula_id`，新一轮只更换运行与结果
  身份；增减、替换或改量时不复用旧方剂身份；
- 输入方、参考方、候选方、R2 建议和沙盘方不互相冒充；
- 病例与轮次不串联；
- 直接原文的典籍、路径、行号和短摘录真实；
- 原文、残文、未取回、相冲和模型泛化身份不混淆；
- 逐字原文与模型解释不混写为同一种来源身份；模型可见材料和读者正文不出现
  内部证据编号；
- 模型已经标记存在未解实质冲突时，结果不冒充 R2 详细建议；
- 方剂条件预测、病例预测、复察预测和已经发生的事实身份不混淆；
- 模型或传输失败不提交半成品、不覆盖上一可用结果；
- `presentation_projection` 与原记录共享同一 `run_id`、`result_id`、任务、主对象、
  病例、方剂、三日内容和来源身份，不新建或改写医学结果身份；
- 纯净版完整吸收 Stage C 对主体内容的保留、修改、排除与未解项；红方攻击全文、
  Stage C 原始修复过程和已失效候选不进入默认 H5／图片内容；
- 用户明确要求后生成的 H5、图形或导出只能读取已经完成同源、守恒和最终结果
  身份校验的 `presentation_projection`；纯净版或扩展生成失败不改变、不覆盖也不
  阻塞正文结果；
- H5 只读报告不能确认事实、激活病例、修改沙盘、触发医学推理或产生新的规范结果。

### 9.2 语义来源与表达原则：通过作品验收

- 中医开放结论由宿主主代理、子代理或其他明确的模型推理步骤形成；确定性工具可以辅助和承载，但不能把机械命中或数据重组冒充为模型判断。
- 经典上下文是否选择了与当前问题相关的句、分句或注释单元，由真实作品核对；不得以整条超长原行或简单字符截断代替内容选择，也不得把这项检查实现为固定行长阈值。
- 主体模型、红方和 Stage C 不得向中医分析链主动添加用户未提供的体系外诊断、指标、筛查或裁决框架。该边界零容忍，但由真实作品和隔离语义复核判定，不建设关键词或医学分类器。
- 红军审计以原六维为默认底座，但允许任务内适应和后续产品升级；是否遗漏关键冲突、是否真正改变或约束蓝军结论，由语义样本和隔离复核判断，不做“恰好六项”的机器门。
- 图形可以在文字结果上继续开展模型解释和语义展开；验收关注解释价值、与本轮事实及主体结论的一致性、经典身份是否真实，而不是要求图形只能复制正文。

### 9.3 模型语义：小样本区间验收

Skill 前验收使用 10 个代表作品，不建设统计评测矩阵：

- 经典研读 2 个；
- 方剂分析 3 个，包含无方名和比较；
- 实际病例 3 个，包含模糊事实和事实纠正；
- 同案复察 2 个，包含明确变化和方向未定。

同一作品同时检查以下五个综合维度，不为每个维度另建样本集：

1. 主对象和事实是否守恒；
2. R0、R1、R2 是否形成真实承接；
3. 红方是否发现或排除真正影响结论的问题；
4. 直接原文、证据边界和模型综合是否诚实；
5. 结果是否清楚、可用且没有体系外污染。

一个作品没有机器硬失败，且隔离复核认为整体成立，记为“满意”。10 个作品中：

- 8 个及以上满意：进入绿色；
- 7 个满意：临界，只增加 2 个与失败根因相邻的变形；扩大后至少 9／12 且不再出现同一根因，才进入绿色；
- 6 个及以下满意：不通过。

机器硬失败不能被满意作品数量抵消。语义质量也不能用固定字段数、图数、调用数、
回执数或谱系深度替代。Badcase 先归因到提示职责、上下文内容、证据边界或执行
一致性的共同根因；一般性修复后复测原样本和少量相邻、反向变形。同一根因最多
三轮仍不稳定时，保留材料交人工裁决，不继续扩大矩阵或增加样本专用规则。

## 10. 真实验收

结构测试只证明硬身份和数据边界，不证明中医作品成立。上述 10 个代表作品必须
通过真实 Codex、Claude Code 等宿主 Agent 运行，不得用旧 Web、直接 API、
预装状态或测试替身代替模型结果。

当前整改先以 Codex 内的确定性机器验收和用户直接文字验收为准，不使用
Computer Use 或外部验收代理。真实宿主作品仍须通过可见宿主对话产生，不能以
直接 API、fixture 医学答案或历史结果代替。H5、图像及其界面布局待文字主链
通过后另行设计和验收。

人工试用发现的问题优先于旧验收账本中的通过字段。旧 Web build 曾出现方名必填阻塞、响应体不完整与反复重试失败、长时间停留在模型分析、输出冗长平铺、L0 下沉、古籍超长行进入错误上下文及嵌套调用耗时过长等问题；后续真实作品又发现模型主动引入体系外风险框架。旧截图、记录和代理裁决只能作为迁移与问题证据，不能证明 Skill 已经通过。

代表作品的开放语义由使用者直接人工验收。当前整改不调用外部复核代理；后续
只有用户重新授权且确有出处、调用权或来源身份争议时，才考虑独立来源复核，
不能把外部代理变成固定验收流水线。

## 11. 当前实施顺序

1. **产品与架构冻结**：先确认本文件、理论核、分角色提示词和精简实施架构的职责边界；旧运行与旧 `2.0` 契约只作参考。
2. **机器契约重建**：根据已确认的新基线重新建立活动 Schema、错误信封、正反例、结构测试和验收场景；不得保留旧记录结构后改名，也不得让结构字段代替模型推理。
3. **Skill 最小主线**：机器契约确认后建立独立 Skill；优先使用宿主原生代理能力，本地 Python 只选择性迁移证据、身份、状态、沙盘和渲染能力。新入口不得导入旧 runtime、旧 Codex CLI 调用拓扑或旧 Web 交互语义。
4. **四类真实作品**：依次验证经典、方剂、病例和复察。每个任务只使用有独立语义价值的模型步骤；证据定位是工具调用，不为页面、图形或格式另启医学推理。
5. **文字主输出**：执行中显示真实阶段进度；完成后输出唯一正式结论、完整职责结果、三日演化、边界与短原文来源。
6. **按需扩展**：只有用户明确要求才从同一已提交结果形成并校验
   `presentation_projection`，再由纯净版生成 H5 或图像；其具体界面和验收另行确认。

阶段 0 的正文与来源治理、阶段 2 的确定性证据服务及旧实现中符合本契约的身份、状态和渲染代码可选择性迁移。旧阶段 3—6 的 Web 运行、真实作品和验收记录只用于暴露问题和核对迁移，不继续作为完成阶段。

## 12. 复杂度与范围边界

新增一个模型角色、调用、状态、ID、事件、图、格式或恢复层之前，必须同时回答：

1. 它防止哪一个已经出现或可稳定复现的用户可见错误？
2. 现有提示词、主链、红蓝审计或简单身份校验为什么不能解决？
3. 删除它后，哪条真实用户旅程会失败？

没有具体答案就不得增加。条件性要求不能在实现中自动升级为默认硬门；验收角色不能自动变成运行角色；Stage 名称不能自动变成物理调用。

宿主一次只处理当前一项任务，本地不持久化活动任务状态机。个人单用户产品只
保存明确激活的个人病历、正式结果、必要历史轮次和一个写时复制沙盘。必要状态使用
简单文件与原子提交，不建设多人并发、持久任务队列、数据库、云同步、独立模型
网关、通用恢复系统或工作流。

OCR、完整独立 UI、固定人体底图、PNG／PDF、账户、云服务和通用平台能力继续后置，不是删除。

## 13. 当前变更边界

- D1—D9、动态红蓝审计、默认使用宿主模型与额度以及 R2 医师复核口径不重新讨论。
- 本次只裁剪产品到实现之间的复杂度翻译，不改变经典正文、经典调用权、四类任务、六项能力、Stage 0/A/B/C 或 D6。原 GEM 六维继续作为默认审计底座，但不再被解释为不可演化的固定字段；C06 改为开放的模型图形解释能力。
- 阶段 2 的正文、调用卡、原文定位、回读、SHA、来源身份和降级行为继续作为
  内部证据能力；活动医学记录只保存读者和提交真正需要的短原文与文档锚点，
  不复制其完整 Evidence Schema 或把内部编号带入模型推理。
- 旧阶段 1—6 的结构、代码、真实作品、浏览器报告和代理裁决保留为迁移与问题证据，不作为当前 Skill 的通过证明；账本中的旧 `pass` 和 `READY-FOR-HUMAN-CONFIRMATION` 已撤销当前效力。
- 当前产品语义与精简实施架构已人工确认；旧 `2.0` 机器契约、旧 Web build 和旧开发过程已隔离为参考。下一步重新建立活动机器契约，机器契约人工确认后才创建 Skill 包。
